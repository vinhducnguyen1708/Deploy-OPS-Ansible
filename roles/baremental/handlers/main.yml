---
- name: disable firewalld
  systemd:
    name: firewalld
    state: stopped
    enabled: no

- name: disable networkmanager
  systemd:
    name: NetworkManager.service
    state: stopped
    enabled: no